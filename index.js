const w = 4
const x = 6
const y = '5'
const z = 'cat'

const multiply = (num1, num2) => {
  console.log(`${num1} * ${num2} =`, num1 * num2)
}

const add = (num1, num2) => {
  console.log(`${num1} + ${num2} =`, num1 + num2)
}

multiply(w, x)
multiply(w, y)
multiply(w, d)

add(w, x)
add(w, y)
add(w, z)
